{
  "name": "jewellery-desktop",
  "version": "1.0.0",
  "description": "Ganesh Jewellery Works Desktop Application",
  "main": "dist/main.js",
  "scripts": {
    "start": "tsc && electron .",
    "build": "tsc && copyfiles -u 1 src/index.html src/styles.css dist/"
  },
  "dependencies": {
     "electron": "^22.0.0"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "typescript": "^4.1.3",
    "ts-node": "^latest-version",
    "@types/node": "^latest-version",
    "@types/electron": "^latest-version"
  }
}
